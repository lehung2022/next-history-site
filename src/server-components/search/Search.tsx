import React from "react";
//These things will be called from API. It will come as a list of search results, and re-direct you to the corresponded page

const Search = () => {
  return (
    // This is for server component, don't mistake it for client component
    <div>Search</div>
  );
};

export default Search;
